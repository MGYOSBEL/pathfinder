apiVersion: v1
kind: Service
metadata:
  name: influxdb-explorer
  namespace: platform
  labels:
    app: influxdb-explorer
spec:
  type: ClusterIP
  selector:
    app: influxdb-explorer
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
