apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: platform
  labels:
    app: influxdb
spec:
  type: ClusterIP
  selector:
    app: influxdb
  ports:
    - name: http
      port: 8181
      targetPort: http
      protocol: TCP
