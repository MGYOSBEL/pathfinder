apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# InfluxDB 3 Core + Explorer UI — raw Kubernetes manifests.
# No official Helm chart exists for influxdb:3-core yet.
# Update this kustomization once an official chart is published.

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - explorer-configmap.yaml
  - explorer-deployment.yaml
  - explorer-service.yaml
