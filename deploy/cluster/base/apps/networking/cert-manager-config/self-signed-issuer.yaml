# Self-signed ClusterIssuer for local development.
# Used to generate TLS certificates for *.pathfinder.local.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
spec:
  selfSigned: {}
