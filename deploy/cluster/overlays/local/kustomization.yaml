apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Add app bases and per-env patches here as real apps are introduced.
# Example:
#   resources:
#     - ../../base/apps/metrics-injector
#
#   patches:
#     - patch: |-
#         - op: replace
#           path: /spec/replicas
#           value: 1
#       target:
#         kind: Deployment
#         name: metrics-injector

labels:
  - pairs:
      env: local
